{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "CosmosDb": {
    "Account": "Set during deployment",
    "Key": "Set during deployment",
    "DatabaseName": "Set during deployment",
    "ContainerName": "Set during deployment"
  }
}

